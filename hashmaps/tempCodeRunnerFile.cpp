for(it = m.begin(); it!= m.end(); it++){
    //     map<int, int>::iterator it1 = m.find(x+it->first);
    //     map<int, int>::iterator it2 = m.find(x-it->first);
    //     if(it1 != m.end()){
    //         cout<<it->first << " - "<<it1->first;
    //         count = it->second * it1->second;
    //     }
    //     if(it2 != m.end()){
    //         cout<<it->first << " - "<<it2->first;
    //         count = it->second * it2->second;    
    //     }
    //     m.erase(it->first);
    // }